/**
 * Sanity data lives in a cloud referred to as "The Content Lake" within datasets.
 * A dataset is a collection of json documents of different types and references.
 * When building schemas - you'll be able to to see the raw objects of the data.
 */

export const schemaTypes = []
